<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Show_Complete_Update_Lead__c</fullName>
    <formula>IF(
    AND(
        NOT(ISPICKVAL(Status__c, &apos;Completed&apos;)),
        NOT(ISPICKVAL(Status__c, &apos;QA Review Completed&apos;)),
        NOT(ISPICKVAL(Status__c, &apos;QA Rejection Complete&apos;))
    ),
    true,
    false
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Show Complete/Update Lead</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
