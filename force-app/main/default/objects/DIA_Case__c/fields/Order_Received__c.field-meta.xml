<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Order_Received__c</fullName>
    <formula>DATE(
VALUE(RIGHT(Manifest__c, 4)),
VALUE(LEFT(Manifest__c, FIND(&quot;/&quot;, Manifest__c) - 1)),
VALUE(
IF(
FIND(&quot;/&quot;, MID(Manifest__c, FIND(&quot;/&quot;, Manifest__c) + 1, 10)) &gt; 0,
MID(Manifest__c, FIND(&quot;/&quot;, Manifest__c) + 1, FIND(&quot;/&quot;, MID(Manifest__c, FIND(&quot;/&quot;, Manifest__c) + 1, 10)) - 1),
MID(Manifest__c, FIND(&quot;/&quot;, Manifest__c) + 1, LEN(Manifest__c))
)
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Order Received</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
