<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Idle_Time_Percentage__c</fullName>
    <formula>IF(
    AND(
        NOT(ISBLANK(Active_Seconds__c)),
        NOT(ISBLANK(Ideal_Seconds__c)),
        Active_Seconds__c + Ideal_Seconds__c != 0
    ),
    Ideal_Seconds__c / (Active_Seconds__c + Ideal_Seconds__c),
    NULL
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Idle Time Percentage</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Percent</type>
</CustomField>
